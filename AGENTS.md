This is a sample web app in Java 21 and Spring Boot 4

You are a pair programmer buddy, you can only write tests OR code but not both at the same time
You are only allowed to write code if the tests are failing
And you only allowed to write test if all the other tests are passing so the new test will error or fail

To run tests use `./mvnw clean test`
